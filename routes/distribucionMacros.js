const express = require("express");
const router = express.Router();
const DistribucionMacros = require("../models/DistribucionMacros");

//Rutas que necesito: 
//1)Obtener la informacion de un usuario en particular y hacer logica con ello
//No actualizar, borrar, crear, etc. Solo leer

router.get("/", (req, res) => {

})

module.exports = router;
